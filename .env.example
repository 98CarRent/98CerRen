ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
JWT_SECRET=98CarRent_Super_Secret_Change_Me_2026
# LINE_NOTIFY_TOKEN=ใส่ Token จาก https://notify-bot.line.me (เมื่อตั้งแล้ว)
# ได้ token: เข้า LINE Notify -> แจ้งเตือน -> หน้า My page -> Generate token
# เพื่อแจ้งเตือนจองใหม่เข้า LINE ของร้าน

# ---- สำหรับ deploy บน Vercel (ถ้าไม่มี ระบบใช้ SQLite ไฟล์ + ดิสก์ท้องถิ่นตามเดิม) ----
# TURSO_URL=libsql://<database>.turso.io
# TURSO_AUTH_TOKEN=<token จาก Turso>
# BLOB_READ_WRITE_TOKEN=<Vercel จะใส่ให้อัตโนมัติเมื่อเพิ่ม Blob Store>